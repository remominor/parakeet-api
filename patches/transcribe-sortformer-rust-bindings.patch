diff --git a/bindings/rust/sys/src/transcribe_sys.rs b/bindings/rust/sys/src/transcribe_sys.rs
index 363cd6e3..1b9597e6 100644
--- a/bindings/rust/sys/src/transcribe_sys.rs
+++ b/bindings/rust/sys/src/transcribe_sys.rs
@@ -1,11 +1,11 @@
 // @generated by `cargo xtask bindgen` from include/transcribe/extensions.h
 // DO NOT EDIT BY HAND. Regenerate: `cargo xtask bindgen`.
-// Pinned to include/transcribe.abihash = 7df72bf9e667b8c2
+// Pinned to include/transcribe.abihash = 0f76382a88042372
 
 /// The public-ABI digest these bindings were generated against
 /// (sha256/16 over the normalized FFI surface). The load-time version
 /// gate and the CI drift check both anchor on this value.
-pub const PUBLIC_HEADER_HASH: &str = "7df72bf9e667b8c2";
+pub const PUBLIC_HEADER_HASH: &str = "0f76382a88042372";
 
 /* automatically generated by rust-bindgen 0.72.1 */
 
@@ -1176,11 +1176,21 @@ impl transcribe_sortformer_preset {
 #[repr(transparent)]
 #[derive(Debug, Copy, Clone, Hash, PartialEq, Eq)]
 pub struct transcribe_sortformer_preset(pub ::std::os::raw::c_uint);
+impl transcribe_sortformer_state_mode {
+    pub const TRANSCRIBE_SORTFORMER_STATE_RESET: transcribe_sortformer_state_mode =
+        transcribe_sortformer_state_mode(0);
+    pub const TRANSCRIBE_SORTFORMER_STATE_PRESERVE: transcribe_sortformer_state_mode =
+        transcribe_sortformer_state_mode(1);
+}
+#[repr(transparent)]
+#[derive(Debug, Copy, Clone, Hash, PartialEq, Eq)]
+pub struct transcribe_sortformer_state_mode(pub ::std::os::raw::c_uint);
 #[repr(C)]
 #[derive(Debug, Copy, Clone)]
 pub struct transcribe_sortformer_stream_ext {
     pub ext: transcribe_ext,
     pub preset: transcribe_sortformer_preset,
+    pub state_mode: transcribe_sortformer_state_mode,
 }
 #[allow(clippy::unnecessary_operation, clippy::identity_op)]
 const _: () = {
@@ -1192,10 +1202,16 @@ const _: () = {
         [::std::mem::offset_of!(transcribe_sortformer_stream_ext, ext) - 0usize];
     ["Offset of field: transcribe_sortformer_stream_ext::preset"]
         [::std::mem::offset_of!(transcribe_sortformer_stream_ext, preset) - 16usize];
+    ["Offset of field: transcribe_sortformer_stream_ext::state_mode"]
+        [::std::mem::offset_of!(transcribe_sortformer_stream_ext, state_mode) - 20usize];
 };
 unsafe extern "C" {
     pub fn transcribe_sortformer_stream_ext_init(ext: *mut transcribe_sortformer_stream_ext);
 }
+unsafe extern "C" {
+    pub fn transcribe_sortformer_reset_state(session: *mut transcribe_session)
+        -> transcribe_status;
+}
 #[repr(C)]
 #[derive(Debug, Copy, Clone)]
 pub struct transcribe_voxtral_realtime_stream_ext {
